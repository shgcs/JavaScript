first.className = "text-dark red";//used to set class names and then can be styling css using this class names
console.log(first.classList);//return all classList
console.log(first.classList.remove("red"));//used to remove the classname
console.log(first.classList.add("red"));//used to add classname
console.log(first.classList.toggle("red"));//if classname is already define then remove classname    and if already not define the add classname
console.log(first.classList.contains("red"));//used to check classname contains is or not
