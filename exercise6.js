
//alert("/****** Welcome in TODO App ******/");
/*
let check = confirm("Do you want to delete or add some TODO list");
 
if (check) {

    let a = prompt("enter 1 for add and 2 for delete");

    if (a == 1) {
        let key = "TODO App" + (localStorage.length + 1);
        let value = prompt("enter the TODO List");

        localStorage.setItem(key, value);

        alert("Item added");
        console.log(key, " ", localStorage.getItem(key));
    }
    if (a == 2) {
        let index = prompt("enter the key index for delete the list");

        let del = "TODO App" + index;
        localStorage.removeItem(del);
        alert("Item deleted");
    }
}

*/