/*
let arr = (size) => {
    let new_array = [];
    for (let i = 0; i < size; i++) {
        let element = prompt(`enter the new element for index ${i}`);
        new_array[i] = element;


    }
    //return new_array;
    for (let i of new_array) {
        document.write("\n");

        document.write(i);
    }
}

let w = prompt("enter length of array");
//document.write(arr(w));
arr(w);*/


/*
let arr = () => {
    let new_array = [];
    let element=1;
    for (let i = 0; element != 0; i++) {
         element = prompt(`enter the new element for index ${i}`);
        new_array[i] = element;


    }
    
  document.write(new_array);
}

arr();*/

/*
let new_arr = (arr) => {

    let w = arr.filter(
        (num) => {
            return num % 10 == 0;
        }
    );

    return w;

}

let arr = [41, 32, 13, 74, 45, 10, 77, 20]

console.log(new_arr(arr));
*/

/*
let new_arr = (arr) => {

    return r = arr.map(      // another way to return 
        (num) => {
            return num * num;
        }
    );

    //return r;

}

let arr = [1, 2, 3, 4, 5, 6, 7, 8, 9];

console.log(new_arr(arr));*/



/*let new_arr = (arr) => {

    let t = arr.reduce(
        (n1, n2) => {

            return n1 * n2;
        }
    );

    return t;

}

let arr = [1, 2, 3, 4, 5, 6, 7, 8, 9];

console.log(new_arr(arr));*/





